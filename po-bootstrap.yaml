apiVersion: v1
kind: Pod
metadata:
  name: bootstrap
  namespace: openshift-logging
  labels:
    app: bootstrap
spec:
  containers:
  - name: bootstrap
    image: docker.io/vladmasarik/hackfest-pod
    env:
    - name: NODE_COUNT
      value: <node-number-placeholder>
    restartPolicy: OnFailure